<nav class="w-full h-16 sticky top-0 bg-white shadow-sm z-50">
  <div class="max-w-7xl mx-auto h-full px-6 flex items-center justify-between">
    <%= link_to root_path do %>
      <h1 class="text-4xl font-extralight text-slate-800">
        Tribu
      </h1>
    <% end %>

    <% if current_user %>
      <div class="flex items-center space-x-6">
        <span class="text-gray-600 font-medium">
          <%= current_user.username %>
        </span>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-sm text-red-600 hover:underline" %>
      </div>
    <% else %>
      <div class="space-x-4">
        <%= link_to "Login", new_user_session_path, class: "text-sm text-gray-600 hover:underline" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "px-3 py-1 text-sm text-white bg-gradient-to-r from-yellow-400 to-red-600 rounded-full hover:from-red-600 hover:to-yellow-400 transition" %>
      </div>
    <% end %>
  </div>
</nav>
