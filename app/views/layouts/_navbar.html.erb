<nav class="w-full h-16 sticky top-0 bg-white shadow-sm z-50">
  <div class="max-w-7xl mx-auto h-full px-6 flex items-center justify-between">
    <%= link_to root_path do %>
      <h1 class="text-4xl font-extralight text-slate-800">
        Tribu
      </h1>
    <% end %>

    <% if current_user %>
      <div class="relative" data-controller="dropdown" data-action="click@window->dropdown#closeUnlessDropdown" >
        <p class="text-text-slate-700 hover:text-slate-900 font-medium" data-action="click->dropdown#toggle"><%= current_user.username %></p>

        <div class="absolute top-12 right-0 w-60 bg-white border border-gray-200 rounded-xl shadow-lg hidden py-2" data-dropdown-target="menu">
          <%= link_to "Edit account", edit_user_registration_path, class: "block px-4 py-2 text-sm text-slate-700 hover:bg-slate-100" %>
          <%= link_to "Sign out", destroy_user_session_path, data: {turbo_method: :delete}, class: "block px-4 py-2 text-sm text-red-600 hover:bg-red-100" %>
        </div>

      </div>
    <% end %>
  </div>
</nav>
